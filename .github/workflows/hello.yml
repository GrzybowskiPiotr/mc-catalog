name: Hello world!

on:
  workflow_dispatch:

jobs:
  add-setup:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: action/checkout@v4

      - name: Configure node
        uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'

      - name: Install dependencies
        run: npm ci

      - name: Greeting
        run: echo "Brawo! nakomita robora!" >> $GITHUB_STEP_SUMMARY
